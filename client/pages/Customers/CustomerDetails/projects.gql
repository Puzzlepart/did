query($customerKey: String) {
  projects(customerKey: $customerKey, sortBy: "name") {
    tag
    key
    name
    icon
    inactive
    labels {
      name
      description
      color
      icon
    }
  }
}
