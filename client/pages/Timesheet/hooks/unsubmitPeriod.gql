mutation UnsubmitPeriod ($opId: String, $period: TimesheetPeriodInput!, $options: TimesheetOptions!) {
  result: unsubmitPeriod(opId: $opId, period: $period, options: $options) {
    success
    opId
    duplicate
    error {
      message
    }
  }
}
